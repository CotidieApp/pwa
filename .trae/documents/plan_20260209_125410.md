He revisado los archivos que subiste y están en perfecto estado (formato TypeScript correcto y contenido completo). Aquí está el plan definitivo de ejecución:

**1. Actualización del "Santo Rosario"**

* **Fuente:** `AllRosary/AllRosary.ts`
* **Acción:** Reemplazaré el archivo actual `src/lib/prayers/plan-de-vida/santo-rosario/index.ts` con el contenido completo de tu archivo.
* **Resultado:** Esto unificará todos los misterios y letanías en una sola estructura coherente, tal como la definiste, eliminando la dependencia de archivos dispersos si así lo prefieres (o los mantendré si tu archivo los referencia, pero veo que tu archivo ya contiene *todo* el objeto `santoRosario`).

**2. Implementación de "Vía Crucis" y "Preces"**

* **Vía Crucis:** Copiaré el contenido de `Via Crucis y Preces/ViaCrucis/via-crucis.ts` a su ubicación definitiva en `src/lib/prayers/plan-de-vida/via-crucis.ts`.
* **Preces:**
  * Copiaré los datos a `src/lib/prayers/plan-de-vida/preces.ts`.
  * **Nota Importante:** Vi que incluiste un `PrayerDetail.tsx` en la carpeta de Preces. Esto indica que probablemente necesitemos actualizar el componente visual para soportar el formato bilingüe (Latín/Español). Analizaré ese archivo y actualizaré el componente de la app si es necesario.

**3. Refactorización de "Ajustes" (Plan "Recordable")**

Dividiré el archivo `Settings.tsx` en 4 sub-secciones claras con nombres cortos para la navegación:

1.  **"Visual"** (`AppearanceSettings`): Temas, fuentes, tamaño de letra, fondos.
2.  **"Alertas"** (`NotificationSettings`): Temporizador y recordatorios diarios.
3.  **"Datos"** (`ContentSettings`): Gestión de oraciones, citas, exportar/importar y restauración.
4.  **"Admin"** (`DeveloperSettings`): Panel de desarrollador y estadísticas avanzadas.

**Orden de Ejecución:**

1.  Aplicar los cambios de contenido (Rosario, Vía Crucis, Preces) para asegurar que los datos estén listos.
2.  Verificar/Actualizar `PrayerDetail.tsx` para las Preces.
3.  Refactorizar la pantalla de Ajustes.

¿Procedo con este plan?
